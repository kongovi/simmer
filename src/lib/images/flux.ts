/**
 * Flux adapter stub — wired in Session 8 when user-level API keys are set.
 */
export async function callFlux(_prompt: string, _recipeId: string): Promise<string> {
  console.warn('Flux adapter not yet implemented')
  return ''
}
