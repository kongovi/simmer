/**
 * DALL-E 3 adapter stub — wired in Session 8 when user-level API keys are set.
 */
export async function callDallE3(_prompt: string, _recipeId: string): Promise<string> {
  console.warn('DALL-E 3 adapter not yet implemented')
  return ''
}
